# Security

Use Android Keystore for cryptographic material. Imported FEN/PGN and engine metadata are untrusted inputs. Release builds enable R8 shrinking. Native binaries should be validated by version/build metadata before activation in a production deployment.
