# Versioning

App releases use semantic versioning at the product level. Database, engine configuration and persisted feature flags are versioned independently and must migrate forward before activation.
